# Codexa web app environment
#
# Copy this file to `.env.local` (kept out of git). Next.js loads it
# automatically. Only NEXT_PUBLIC_* values are exposed to the browser.

# Base URL of the Codexa API server.
NEXT_PUBLIC_API_BASE=http://localhost:4000
